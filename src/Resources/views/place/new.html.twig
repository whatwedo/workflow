{% extends '@whatwedoWorkflow/layout.html.twig' %}

{% block body %}
        {% embed "include/embed/portlet.html.twig" with { portlet_title: 'Create new Place'} %}
    {% block portlet_body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        <a class="btn btn-default" href="{{ path('wwd_workflow_workflow_show', {id: place.workflow.id}) }}">back to Workflow</a>
    {{ form_end(form) }}


    {% endblock %}
    {% endembed %}
{% endblock %}

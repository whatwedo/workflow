<form method="post" action="{{ path('workflow_transition_event_definition_delete', {'id': transition_event_definition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transition_event_definition.id) }}">
    <button class="btn">Delete</button>
</form>

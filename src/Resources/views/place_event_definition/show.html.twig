{% extends '@whatwedoWorkflow/layout.html.twig' %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place_event_definition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ place_event_definition.name }}</td>
            </tr>
            <tr>
                <th>EventName</th>
                <td>{{ place_event_definition.eventName }}</td>
            </tr>
            <tr>
                <th>Sortorder</th>
                <td>{{ place_event_definition.sortorder }}</td>
            </tr>
            <tr>
                <th>Expression</th>
                <td>{{ place_event_definition.expression }}</td>
            </tr>
            <tr>
                <th>Template</th>
                <td>{{ place_event_definition.template }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wwd_workflow_workflow_show', {id:place_event_definition.place.workflow.id }) }}">back to list</a>

    <a href="{{ path('wwd_workflow_place_event_definition_edit', {'id': place_event_definition.id}) }}">edit</a>

    {{ include('workflow_place_event_definition/_delete_form.html.twig') }}

    {% endblock %}

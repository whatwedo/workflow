{% extends 'baseMet.html.twig' %}
{% import "macros/elements.html.twig" as elements %}

{% block body %}
            {% embed "include/embed/portlet.html.twig" with { portlet_title: 'Workflow'} %}
    {% block portlet_body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        <a class="btn btn-default" href="{{ path('workflow_workflow_index') }}">back to list</a>
    {{ form_end(form) }}


        {% endblock %}
        {% endembed %}
{% endblock %}
